services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - backend-data:/app/data
    environment:
      - DATABASE_URL=sqlite:///data/every_note.db

  frontend:
    build: ./apps/web
    ports:
      - "5173:5173"
    volumes:
      - ./apps/web/src:/app/src
    environment:
      - API_URL=http://backend:8000
    depends_on:
      - backend

volumes:
  backend-data:
